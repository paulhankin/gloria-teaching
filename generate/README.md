# Local worksheet repositories

Each `generate/<username>/` directory is an independent local Git repository.
These repositories are ignored by the main project and are created by the
worksheet request pipeline when needed.
